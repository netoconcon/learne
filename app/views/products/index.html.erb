<h2>Nova pagina de produtos</h2>

<p>
  Meus produtos
  <%= link_to "Cadastrar novo produto", new_product_path %>
</p>

<% @products.each do |product| %>
  <p>
    <%= product.id %>
    imagem
    <%= product.sku %>
    <%= product.name %>
    <%= product.price %>
    <%= link_to "Editar", edit_product_path(product) %>
    <%= link_to "Apagar produto", product_path(product), method: :delete, data: { confirm: 'Você tem certeza que deseja apagar o produto?' } %>
  </p>
<% end %>